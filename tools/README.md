# Tools

## Eye Preview

Run this from the project folder:

```sh
node tools/serve-preview.mjs
```

Then open:

```text
http://localhost:4173/tools/eye-preview.html
```

The preview paints the current WLED `seg.i` ranges onto a 64x8 grid and shows the exact JSON block to paste into WLED.
